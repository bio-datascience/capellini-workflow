# proGenomes4 spacer collection — DUMMY PLACEHOLDER (empty).
# Replace spacers_CompleteCollection.fasta with a MinCED spacer collection
# extracted from pg4_genomes_representatives.fna.gz (headers must start with the
# versioned GCA, e.g. >GCA_000005825.2_CP001878.2_...).
